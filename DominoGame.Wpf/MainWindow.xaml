<Window x:Class="DominoGame.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:DominoGame.Wpf"
        xmlns:ctrl="clr-namespace:DominoGame.Wpf.Controls"
        Title="Domino Game"
        Width="980"
        Height="640"
        Background="#0E2E2F">

    <Window.Resources>
        <DataTemplate x:Key="HandTileTemplateHorizontal">
            <Grid x:Name="TileRoot" Margin="3">
                <Grid x:Name="FaceUpRoot">
                    <ctrl:DominoTileControl/>
                </Grid>
                <Border x:Name="FaceDownRoot"
                        Width="35"
                        Height="70"
                        BorderThickness="2"
                        BorderBrush="Black"
                        Background="#F1F7F6"
                        CornerRadius="4"/>
            </Grid>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding DataContext.IsCurrent, RelativeSource={RelativeSource AncestorType=ItemsControl}}" Value="True">
                    <Setter TargetName="FaceUpRoot" Property="Visibility" Value="Visible"/>
                    <Setter TargetName="FaceDownRoot" Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding DataContext.IsCurrent, RelativeSource={RelativeSource AncestorType=ItemsControl}}" Value="False">
                    <Setter TargetName="FaceUpRoot" Property="Visibility" Value="Collapsed"/>
                    <Setter TargetName="FaceDownRoot" Property="Visibility" Value="Visible"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsPlayable}" Value="False">
                    <Setter TargetName="TileRoot" Property="Opacity" Value="0.5"/>
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>

        <DataTemplate x:Key="HandTileTemplateVertical">
            <Grid x:Name="TileRoot" Margin="3">
                <Grid.LayoutTransform>
                    <RotateTransform Angle="90"/>
                </Grid.LayoutTransform>
                <Grid x:Name="FaceUpRoot">
                    <ctrl:DominoTileControl/>
                </Grid>
                <Border x:Name="FaceDownRoot"
                        Width="35"
                        Height="70"
                        BorderThickness="2"
                        BorderBrush="Black"
                        Background="#F1F7F6"
                        CornerRadius="4"/>
            </Grid>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding DataContext.IsCurrent, RelativeSource={RelativeSource AncestorType=ItemsControl}}" Value="True">
                    <Setter TargetName="FaceUpRoot" Property="Visibility" Value="Visible"/>
                    <Setter TargetName="FaceDownRoot" Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding DataContext.IsCurrent, RelativeSource={RelativeSource AncestorType=ItemsControl}}" Value="False">
                    <Setter TargetName="FaceUpRoot" Property="Visibility" Value="Collapsed"/>
                    <Setter TargetName="FaceDownRoot" Property="Visibility" Value="Visible"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsPlayable}" Value="False">
                    <Setter TargetName="TileRoot" Property="Opacity" Value="0.5"/>
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>

        <DataTemplate x:Key="PlayerHandHorizontalTemplate">
            <ItemsControl ItemsSource="{Binding Hand}" ItemTemplate="{StaticResource HandTileTemplateHorizontal}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <EventSetter Event="PreviewMouseLeftButtonDown" Handler="HandTile_PreviewMouseLeftButtonDown"/>
                        <EventSetter Event="PreviewMouseMove" Handler="HandTile_PreviewMouseMove"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>
            </ItemsControl>
        </DataTemplate>

        <DataTemplate x:Key="PlayerHandVerticalTemplate">
            <ItemsControl ItemsSource="{Binding Hand}" ItemTemplate="{StaticResource HandTileTemplateVertical}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <EventSetter Event="PreviewMouseLeftButtonDown" Handler="HandTile_PreviewMouseLeftButtonDown"/>
                        <EventSetter Event="PreviewMouseMove" Handler="HandTile_PreviewMouseMove"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>
            </ItemsControl>
        </DataTemplate>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="240"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Grid.ColumnSpan="2"
                Padding="12"
                Background="#123B3D"
                CornerRadius="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="{Binding StatusMessage}"
                           Foreground="#F1F7F6"
                           FontSize="14"
                           VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Turn:"
                               Foreground="#CDE5E3"
                               Margin="0,0,6,0"/>
                    <TextBlock Text="{Binding CurrentPlayerName}"
                               Foreground="#F1F7F6"
                               FontWeight="Bold"
                               Margin="0,0,12,0"/>
                    <TextBlock Text="Score:"
                               Foreground="#CDE5E3"
                               Margin="0,0,6,0"/>
                    <TextBlock Text="{Binding CurrentPlayerScore}"
                               Foreground="#F1F7F6"
                               FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1"
                Grid.Column="0"
                Margin="0,18,0,18"
                Background="#184B4E"
                CornerRadius="14"
                Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ContentControl Grid.Row="0"
                                Grid.Column="1"
                                Content="{Binding TopPlayer}"
                                ContentTemplate="{StaticResource PlayerHandHorizontalTemplate}"/>

                <ContentControl Grid.Row="2"
                                Grid.Column="1"
                                Content="{Binding BottomPlayer}"
                                ContentTemplate="{StaticResource PlayerHandHorizontalTemplate}"/>

                <ContentControl Grid.Row="1"
                                Grid.Column="0"
                                Content="{Binding LeftPlayer}"
                                ContentTemplate="{StaticResource PlayerHandVerticalTemplate}"/>

                <ContentControl Grid.Row="1"
                                Grid.Column="2"
                                Content="{Binding RightPlayer}"
                                ContentTemplate="{StaticResource PlayerHandVerticalTemplate}"/>

                <Border Grid.Row="1"
                        Grid.Column="1"
                        Background="#1B4F52"
                        CornerRadius="12"
                        Padding="12"
                        Margin="10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Board"
                                   Foreground="#DFF3F1"
                                   FontWeight="Bold"
                                   Margin="0,0,0,10"/>

                        <Grid Grid.Row="1"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center">
                            <Grid x:Name="BoardArea"
                                  Width="835"
                                  Height="400">
                                <ItemsControl x:Name="BoardItems"
                                              ItemsSource="{Binding BoardDominoes}"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center">
                                    <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <ctrl:OShapePanel Padding="16" ItemSpacing="3"/>
                                    </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <ctrl:DominoTileControl/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <Canvas x:Name="BoardOverlay"
                                        Panel.ZIndex="2">
                                    <Border x:Name="DropLeftZone"
                                            Width="35"
                                            Height="70"
                                            Background="#123B3D"
                                            CornerRadius="8"
                                            Opacity="0.9"
                                            AllowDrop="True"
                                            DragOver="DropZone_DragOver"
                                            Drop="DropLeftZone_Drop">
                                        <TextBlock Text="Left"
                                                   Foreground="#CDE5E3"
                                                   FontSize="11"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"/>
                                    </Border>
                                    <Border x:Name="DropRightZone"
                                            Width="35"
                                            Height="70"
                                            Background="#123B3D"
                                            CornerRadius="8"
                                            Opacity="0.9"
                                            AllowDrop="True"
                                            DragOver="DropZone_DragOver"
                                            Drop="DropRightZone_Drop">
                                        <TextBlock Text="Right"
                                                   Foreground="#CDE5E3"
                                                   FontSize="11"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"/>
                                    </Border>
                                </Canvas>
                            </Grid>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <Border Grid.Row="1"
                Grid.Column="1"
                Margin="16,18,0,18"
                Background="#123B3D"
                CornerRadius="12"
                Padding="12">
            <StackPanel>
                <TextBlock Text="Scoreboard"
                           Foreground="#DFF3F1"
                           FontWeight="Bold"
                           Margin="0,0,0,10"/>

                <ItemsControl ItemsSource="{Binding Players}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="{Binding Name}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#F1F7F6"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsCurrent}" Value="True">
                                                    <Setter Property="Foreground" Value="#F2D06B"/>
                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock Grid.Column="1"
                                           Text="{Binding Score}"
                                           Foreground="#F2D06B"
                                           FontWeight="Bold"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>
    </Grid>
</Window>
