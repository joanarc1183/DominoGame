<Window x:Class="DominoGame.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:DominoGame.Wpf"
        Title="Domino Game" Width="800" Height="450">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Board -->
        <ItemsControl Grid.Row="0" ItemsSource="{Binding Board}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="1" Padding="5" Margin="2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Left}"/>
                            <TextBlock Text="|"/>
                            <TextBlock Text="{Binding Right}"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Hand -->
        <ItemsControl Grid.Row="1" ItemsSource="{Binding Hand}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Command="{Binding DataContext.PlayDominoCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                        CommandParameter="{Binding}"
                        IsEnabled="{Binding DataContext.PlayableTiles, RelativeSource={RelativeSource AncestorType=Window}, 
                                    Converter={StaticResource DominoEnabledConverter}, ConverterParameter={Binding}}">

                        <Border BorderThickness="2" BorderBrush="Black" Padding="6">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Left}"/>
                                <TextBlock Text="|"/>
                                <TextBlock Text="{Binding Right}"/>
                            </StackPanel>
                        </Border>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Window>
